[project]
name = "llm-usage-eval"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "datasets",
    "requests",
    "python-dotenv",
    "openhands-sdk",
    "openhands-tools",
    "openpyxl",
    "tqdm",
    "pandas",
    "python-docx",
    "PyPDF2",
    "pdfplumber",
    "scipy",
    "numpy",
    "reportlab",  # For PDF generation
]

[tool.uv]
# allows uv sync to work
